{
  "name": "VR-Toggler",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . VRToggler --platform=win32 --arch=x64 --icon=icon.ico --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
